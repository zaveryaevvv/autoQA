text_input: &text_input
  answer: Сбер карта
  description: Обычное текстовое поле
  required: null
  selector: null

search_input: &search_input
   answer: Кредитная карта
   description: Поиск в плоском справочнике
   required: null
   order: 1
   selector: null

person_input: &person_input
  answer: 
    first_name: Alex
    second_name: Zaveryaev
    employee_id: 21100018
  description: Поиск человека по ФИО или табельнику
  selector: null


РИСК_ПОВЕДЕНИЯ:
  ОЦЕНКА:
    ИНПУТ:
      ИМЯ_ПРОДУКТА: &РИСК_ПОВЕДЕНИЯ.ОЦЕНКА.ИНПУТ.ИМЯ_ПРОДУКТА
        type: *text_input
        answer: Кредитная карта
        required: yes
        selector:
          id: product_name


    КНОПКА:
      ДАЛЕЕ: &РИСК_ПОВЕДЕНИЯ.ОЦЕНКА.КНОПКА.ДАЛЕЕ
        selector: 
          id: next_button
      ОТМЕНИТЬ: &РИСК_ПОВЕДЕНИЯ.ОЦЕНКА.КНОПКА.ОТМЕНИТЬ
        selector:
          id: cancel_button



production:
  <<: *defaults  # Подключаем якорь "defaults" повторно
  host:  # Перезаписываем параметр "host"
